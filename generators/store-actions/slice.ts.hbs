import { createSlice } from '@reduxjs/toolkit';
import { RootState } from '../../store';

export interface {{pascalCase name}} {}

const initialState: {{pascalCase name}} = {};

const {{camelCase name}}Slice = createSlice({
	name: '{{camelCase name}}',
	initialState,
	reducers: {},
});

// Actions
export const {{camelCase name}}Actions = {{camelCase name}}Slice.actions;

// Selectors
export const {{camelCase name}} = (state: RootState) => state.{{camelCase name}};

// Reducer
const {{camelCase name}}Reducer = {{camelCase name}}Slice.reducer;
export default {{camelCase name}}Reducer;
